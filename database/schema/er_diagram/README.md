# Entity Relationship Diagram

Place the exported ER diagram here (e.g. `er_diagram.png` or a
`.drawio`/`.excalidraw` source file) once the schema is finalized.

Until then, see `docs/database-design.md` at the repo root for the
full entity list, fields, and relationships in text/table form.
