// TODO: Committed vs Achieved pie chart for the Budget Report page.
// Consider a lightweight lib or plain SVG — avoid adding a heavy
// charting dependency for a 24-hour hackathon.
export default function BudgetPieChart({ committed, achieved }) {
  return <div>Budget Pie Chart — TODO (committed={committed}, achieved={achieved})</div>
}
