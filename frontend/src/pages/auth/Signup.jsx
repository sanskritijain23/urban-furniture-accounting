// Route: /signup
// Public sign-up. Always creates a role=accountant user (server-enforced).
// TODO: form fields — Login ID, Email, Password, Re-enter Password.
export default function Signup() {
  return <div>Sign Up Page — TODO</div>
}
