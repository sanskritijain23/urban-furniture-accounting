// Routes: /products/new, /products/:id
// TODO: Product Name, Type (Goods/Service/Combo), Category (inline-creatable),
// Sales Price, Cost, Upload Image.
export default function ProductForm() {
  return <div>Product Form — TODO</div>
}
