// Route: /purchases/bills/:id
// TODO: Bill No/Reference/Vendor/Dates, line items, New/Confirm/Pay
// buttons, Payment modal on Pay click. Confirming DOES create a
// Journal Entry (server-side only — no client accounting math here).
export default function VendorBillForm() {
  return <div>Vendor Bill Form — TODO</div>
}
